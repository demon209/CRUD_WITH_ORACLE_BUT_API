@model ProductListViewModel

<h2>Danh sách sản phẩm</h2>
<table class="table">
    <thead>
        <tr>
            <th>Product ID</th>
            <th>Tên sản phẩm</th>
            <th>Loại</th>
            <th>Giá</th>
            <th>Số lượng</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @if (Model.Products == null || Model.Products.Count == 0)
        {
            <tr>
                <td colspan="5" class="text-center">Không có sản phẩm nào.</td>
            </tr>
        }
        else
        {
            @foreach (var product in Model.Products)
            {
                <tr>
                    <td>@product.ProductId</td>
                    <td>@product.ProductName</td>
                    <td>@product.ProductCategory</td>
                    <td>@product.ProductPrice</td>
                    <td>@product.ProductStock</td>
                    <td>
                        <div class="d-flex gap-2">
                            <form asp-action="DeleteProduct" asp-controller="Product" method="post"
                                onsubmit="return confirm('Bạn có chắc muốn xóa?');" class="d-inline">
                                <input type="hidden" name="id" value="@product.ProductId" />
                                <input type="hidden" name="page" value="@Model.CurrentPage" /> <!-- 👈 thêm dòng này -->
                                <button type="submit" class="btn btn-danger">🙅‍♂️ Xóa</button>
                            </form>
                            <a asp-action="EditProduct" asp-route-id="@product.ProductId" asp-route-page="@Model.CurrentPage" class="btn btn-warning">👨‍🔧 Sửa</a>
                        </div>
                    </td>

                </tr>
            }
        }
    </tbody>
</table>
<a asp-action="CreateProducts" asp-controller="Product" class="btn btn-success mb-3">🛒 Thêm sản phẩm</a>

<!-- Phân trang -->
@await Html.PartialAsync("_Pagination", Model)
