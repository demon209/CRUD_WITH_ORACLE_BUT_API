@model OrderDetailViewModel

<div class="mb-3">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-start align-items-md-center">
        <h2 class="mb-2 mb-md-0">📦 Chi tiết đơn hàng có ID: @Model.Order.OrderId</h2>
        <a href="/HoaDon" class="btn btn-secondary">Quay lại trang hóa đơn</a>
    </div>
</div>



<hr />
<div class="container mt-4">
    <div class="card shadow-sm rounded-3">
        <div class="card-body">
            <h3 class="card-title text-primary fw-bold mb-3">
                📜 Thông tin đơn hàng
            </h3>
            <ul class="list-unstyled fs-5">
                <li><strong>Ngày tạo hóa đơn:</strong> @Model.Order.OrderDate.ToString("dd/MM/yyyy")</li>
                <li><strong>Tổng tiền:</strong> @String.Format("{0:N0}",@Model.Order.TotalAmount) VNĐ</li>
            </ul>
        </div>
    </div>
</div>


<div class="container mt-4">
    <div class="card shadow-sm rounded-3">
        <div class="card-body">
            <h3 class="card-title text-primary fw-bold mb-3">
                👤 Thông tin khách hàng
            </h3>
            <ul class="list-unstyled fs-5">
                <li><strong>ID:</strong> @Model.Customer.CustomerId</li>
                <li><strong>Tên:</strong> @Model.Customer.FirstName @Model.Customer.LastName</li>
                <li><strong>Email:</strong> @Model.Customer.Email</li>
                <li><strong>SĐT:</strong> @Model.Customer.PhoneNumber</li>
                <li><strong>Địa chỉ:</strong> @Model.Customer.Address</li>
            </ul>
        </div>
    </div>
</div>

@if (Model.Pet != null)
{
    <div class="container mt-4">
        <div class="card shadow-sm rounded-3">
            <div class="card-body">
                <h3 class="card-title text-primary fw-bold mb-3">
                    🐾 Thông tin thú cưng
                </h3>
                <ul class="list-unstyled fs-5">
                    <li><strong>Ảnh:</strong>
                        @if (!string.IsNullOrEmpty(@Model.Pet.ImageBase64))
                        {
                            <img src="@Model.Pet.ImageBase64" alt="Ảnh thú cưng" width="80" height="80"
                                style="object-fit:cover; border-radius:8px;" />
                        }
                        else
                        {
                            <span class="text-muted">Không có ảnh</span>
                        }
                    </li>
                    <li><strong>ID:</strong> @Model.Pet.PetId</li>
                    <li><strong>Tên:</strong> @Model.Pet.PetName</li>
                    <li><strong>Loài:</strong> @Model.Pet.PetType</li>
                    <li><strong>Giống:</strong> @Model.Pet.Breed</li>
                    <li><strong>Tuổi:</strong> @Model.Pet.Age</li>
                    <li><strong>Giới tính:</strong> @Model.Pet.Gender</li>
                    <li><strong>Giá:</strong> @String.Format("{0:N0}",@Model.Pet.Price) VNĐ</li>
                </ul>
            </div>
        </div>
    </div>
}
else
{
    <div class="container mt-4">
        <div class="card shadow-sm rounded-3">
            <div class="card-body">
                <h3 class="card-title text-primary fw-bold mb-3">
                    🐾 Thông tin thú cưng
                </h3>
                <ul class="list-unstyled fs-5">
                    <li class="text-muted">Không có thú cưng nào liên quan đến đơn hàng này.</li>
                </ul>
            </div>
        </div>
    </div>
}

@if (Model.Product != null)
{
    <div class="container mt-4">
        <div class="card shadow-sm rounded-3">
            <div class="card-body">
                <h3 class="card-title text-primary fw-bold mb-3">
                    📦 Thông tin sản phẩm
                </h3>
                <ul class="list-unstyled fs-5">
                    <li><strong>ID:</strong> @Model.Product.ProductId</li>
                    <li><strong>Tên:</strong> @Model.Product.ProductName</li>
                    <li><strong>Loại:</strong> @Model.Product.ProductCategory</li>
                    <li><strong>Giá:</strong> @Model.Product.ProductPrice.ToString("N0") VNĐ</li>
                    <li><strong>Số lượng:</strong> @Model.Order.ProductQuantity</li>
                    <li><strong>Tổng tiền sản phẩm:</strong> @String.Format("{0:N0}", Model.Product.ProductPrice *
                                            Model.Order.ProductQuantity) VNĐ</li>

            </ul>
        </div>
    </div>
</div>
}
else
{
    <div class="container mt-4">
        <div class="card shadow-sm rounded-3">
            <div class="card-body">
                <h3 class="card-title text-primary fw-bold mb-3">
                    📦 Thông tin sản phẩm
                </h3>
                <ul class="list-unstyled fs-5">
                    <li class="text-muted">Không có sản phẩm nào liên quan đến đơn hàng này.</li>
                </ul>
            </div>
        </div>
    </div>
}
